configuration:

supported_engines:

requires_shotgun_fields:

display_name: "Batch Publisher Texture..."
description: "Batch pulish texture files data to shotgun."

requires_shotgun_version:
requires_core_version: "v0.19.3"
requires_engine_version:

frameworks:
